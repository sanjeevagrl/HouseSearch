import { FilterPropertyPipe } from './filter-property.pipe';

describe('FilterPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
